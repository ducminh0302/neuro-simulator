import type { ReactNode } from "react";

export const metadata = {
  title: "A/B Comparison — Results",
  description: "Head-to-head comparison results for creative variants.",
};

export default function AbCompareResultsLayout({ children }: { children: ReactNode }) {
  return children;
}
