import type { ReactNode } from "react";

export const metadata = {
  title: "New simulation",
  description: "Describe your campaign to start a simulation.",
};

export default function NewSimulationLayout({ children }: { children: ReactNode }) {
  return children;
}
